AuthService Design Document

AuthService is responsible for user authentication across the platform.

Users authenticate by submitting credentials to AuthService.
AuthService validates the credentials and generates a JWT token upon success.
JWT tokens expire after 24 hours and must be refreshed.

AuthService communicates with PostgreSQL to store and retrieve user records.
AuthService exposes authentication endpoints consumed by the API Gateway.
